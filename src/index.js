/**
 * Calculates square of given number.
 * 
 * @param {number} num 
 */
function square( num )
{
}


/**
 * Converts half the string to uppercase and half the string to lowercase.
 * If second parameter specified then it converts first N characters into uppercase,
 * and rest to lowercase.
 * 
 * @param {string} input - input string
 * @param {number} base - number of characters to convert to uppercase
 */
function convertToHalfUpper( input, base )
{
    // Hints: string.toUpperCase(), string.toLowerCase, string.substr()
}


/**
 * Returns an array (= state).
 */
function getState()
{
}


/**
 * Adds value into state. Value can't be empty.
 * 
 * @param {*} value - value to add
 */
function addToState( value )
{
    // Hints: Array.splice(), Array.push()
    if ( ! value ) return; // Hint as well
}


/**
 * Sets state to empty array.
 */
function dropState()
{
}